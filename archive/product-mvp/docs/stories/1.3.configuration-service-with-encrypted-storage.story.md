# Story 1.3: Configuration Service with Encrypted Storage

## Status

Done

## Story

**As a** user,
**I want** my API keys stored securely and encrypted,
**so that** my credentials are protected.

## Acceptance Criteria

1. `ConfigService` class with methods: `initialize`, `set`, `get`, `has`, `delete`
2. Uses `conf` library with encryption in OS-specific locations
3. Config schema defined with TypeScript interface
4. API keys never logged (redaction in place)
5. Environment variable support: `TM_PROVIDER`, `TM_API_KEY`
6. Unit tests cover storage/retrieval, environment precedence

> **Note:** `docs/prd/technical-assumptions.md` lists env vars as `TMR_PROVIDER` / `TMR_API_KEY`, while the epic AC specifies `TM_PROVIDER` / `TM_API_KEY`. Following the same precedence rule as the binary name (`tm` not `tmr`): **use `TM_PROVIDER` and `TM_API_KEY`** as the canonical names.

## Tasks / Subtasks

- [x] Task 1: Install runtime dependencies (AC: 2)
  - [x] `npm install conf` — encrypted config storage (runtime dep, not devDep)
  - [x] `npm install dotenv` — `.env` file loading (runtime dep)
  - [x] Verify both are in `dependencies` in `package.json`

- [x] Task 2: Define config schema (AC: 3)
  - [x] Create `src/types/config.types.ts`
  - [x] Export `AppConfig` interface with all known fields (`provider`, `apiKey`)
  - [x] Export `CONFIG_KEYS` constant (typed array of keyof AppConfig) for future iteration

- [x] Task 3: Implement `ConfigService` class (AC: 1, 2, 3)
  - [x] Create `src/services/config.service.ts`
  - [x] Instantiate `Conf<AppConfig>` with `projectName`, `encryptionKey`, and optional `schema`
  - [x] Implement `initialize()` — bootstraps the store, loads `.env` via dotenv
  - [x] Implement `set<K>(key: K, value: AppConfig[K]): void`
  - [x] Implement `get<K>(key: K): AppConfig[K] | undefined` — env var takes precedence (see Task 5)
  - [x] Implement `has(key: keyof AppConfig): boolean`
  - [x] Implement `delete(key: keyof AppConfig): void`
  - [x] Export a singleton `configService` instance for use in commands

- [x] Task 4: API key redaction (AC: 4)
  - [x] Add `redact(value: string): string` helper in `src/utils/redact.ts`
  - [x] Masks all but the last 4 characters: `sk-ant-...ABCD` → `***ABCD`
  - [x] Ensure no raw API key value is passed to `process.stderr.write` or any logging path inside ConfigService

- [x] Task 5: Environment variable precedence (AC: 5)
  - [x] In `get('provider')` — check `process.env.TM_PROVIDER` first; fall back to stored conf value
  - [x] In `get('apiKey')` — check `process.env.TM_API_KEY` first; fall back to stored conf value
  - [x] `initialize()` calls `dotenv.config()` to load `.env` before any env var reads

- [x] Task 6: Write unit tests (AC: 1, 3, 4, 5, 6)
  - [x] Create `tests/services/config.service.test.ts`
  - [x] Mock `conf` library — do NOT write to real OS config dirs in tests
  - [x] Test: `set` + `get` round-trip returns stored value
  - [x] Test: `has` returns true after `set`, false before
  - [x] Test: `delete` removes stored value
  - [x] Test: `get('provider')` returns `TM_PROVIDER` env var when set (env precedence)
  - [x] Test: `get('apiKey')` returns `TM_API_KEY` env var when set (env precedence)
  - [x] Test: `get` falls back to conf store when env var is not set
  - [x] Test: `redact()` masks all but last 4 chars
  - [x] Test: `redact()` handles short strings safely (≤4 chars → all masked)
  - [x] Create `tests/utils/redact.test.ts`

- [x] Task 7: Validate (AC: 1–6)
  - [x] `npm run validate` → lint + typecheck + all tests green + build

## Dev Notes

### Previous Story Insights (from Story 1.2)

- **ESM is active**: `"type": "module"` — all source imports need `.js` extensions; test imports mapped by `moduleNameMapper` in `jest.config.ts`
- **No `console.log`**: ESLint `no-console` is set to `warn` — use `process.stdout.write` / `process.stderr.write` in any output paths inside the service
- **Runtime deps go in `dependencies`**: `conf` and `dotenv` ship with the binary — must NOT be in `devDependencies`
- **ESM + Jest guard**: `JEST_WORKER_ID` env var is set by Jest workers — use it if any module-level side effects need to be skipped in tests
- **jest@29.7.0 + ts-jest@29.4.6**: Do not upgrade without verifying both together
- **globals.node in ESLint**: `process` and `Buffer` are already covered — no new ESLint config change needed

[Source: docs/stories/1.2.cli-framework-with-commanderjs.story.md#completion-notes-list]

### `conf` Library — Encrypted Storage

`conf` stores config in OS-specific user data directories automatically:

```
macOS:   ~/Library/Application Support/@marlonvidal-tech-leadership-os/
Linux:   ~/.config/@marlonvidal-tech-leadership-os/
Windows: %APPDATA%\@marlonvidal-tech-leadership-os\
```

Encryption is enabled by passing `encryptionKey` to the constructor (AES-256):

```typescript
import Conf from 'conf';
import type { AppConfig } from '../types/config.types.js';

const store = new Conf<AppConfig>({
  projectName: '@marlonvidal/tech-manager-os',
  encryptionKey: 'tm-config-v1', // app-scoped obfuscation key
});
```

> The `encryptionKey` is embedded in the binary — this is "better than plaintext" protection for config files at rest, not cryptographically secret. This is the standard `conf` pattern for CLI tools.

[Source: docs/prd/technical-assumptions.md#security--configuration]

### Environment Variable Precedence

`get()` must check env vars first, then fall back to the conf store. The precedence order:

```
TM_PROVIDER env var  >  conf store value
TM_API_KEY  env var  >  conf store value
```

`initialize()` must call `dotenv.config()` early so `.env` files are loaded before any `get()` call.

```typescript
import dotenv from 'dotenv';

initialize(): void {
  dotenv.config(); // loads .env if present
  // store is already available via Conf constructor
}
```

[Source: docs/prd/technical-assumptions.md#security--configuration]
[Source: docs/prd/epics/epic-1-foundation-cli-infrastructure.md#story-13]

### API Key Redaction

API keys must never appear in logs or error output. A standalone utility is preferred over inline masking:

```typescript
// src/utils/redact.ts
export function redact(value: string): string {
  if (value.length <= 4) return '****';
  return `${'*'.repeat(value.length - 4)}${value.slice(-4)}`;
}
```

All paths inside `ConfigService` that could output the apiKey value (e.g., error messages, debug) must call `redact()`.

[Source: docs/prd/epics/epic-1-foundation-cli-infrastructure.md#story-13-ac4]

### Mocking `conf` in Tests

Do NOT let tests write to real OS config directories. Mock the `conf` module:

```typescript
import { jest } from '@jest/globals';

jest.unstable_mockModule('conf', () => {
  const store = new Map<string, unknown>();
  return {
    default: jest.fn().mockImplementation(() => ({
      get: (key: string) => store.get(key),
      set: (key: string, value: unknown) => store.set(key, value),
      has: (key: string) => store.has(key),
      delete: (key: string) => store.delete(key),
    })),
  };
});
```

Use `jest.unstable_mockModule` (ESM-compatible) instead of `jest.mock` for ESM modules.
Reset mock store state in `beforeEach` to avoid test pollution.
Restore env vars modified in tests using `afterEach` cleanup.

[Source: docs/prd/technical-assumptions.md#testing-requirements]

### File Locations

```
src/
├── services/
│   └── config.service.ts   ← NEW: ConfigService class + singleton export
├── types/
│   └── config.types.ts     ← NEW: AppConfig interface, CONFIG_KEYS constant
└── utils/
    └── redact.ts           ← NEW: redact() helper

tests/
├── services/
│   └── config.service.test.ts  ← NEW
└── utils/
    └── redact.test.ts          ← NEW
```

No changes to `src/cli.ts` in this story — ConfigService will be wired into commands in Story 1.6.

### Runtime vs Dev Dependencies

- `conf` → **`dependencies`** (ships with the binary)
- `dotenv` → **`dependencies`** (loaded at runtime to resolve env vars)

[Source: docs/prd/technical-assumptions.md#technology-stack]

## Testing

- **Framework:** Jest 29 + `ts-jest` (ESM mode via `NODE_OPTIONS=--experimental-vm-modules`)
- **Test files:** `tests/services/config.service.test.ts`, `tests/utils/redact.test.ts`
- **Mocking:** `jest.unstable_mockModule` for ESM-compatible `conf` mocking — do NOT use `jest.mock`
- **Env var isolation:** Set `process.env.TM_PROVIDER` / `process.env.TM_API_KEY` in test scope; restore via `delete process.env.X` in `afterEach`
- **No real I/O:** All conf calls must hit the mock — verify no files are written to `~/Library` or `~/.config` during tests
- **Coverage target:** All 6 ACs covered with at least one test each; all redact edge cases covered

[Source: docs/prd/technical-assumptions.md#testing-requirements]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-02-23 | 1.1 | Implemented — 28/28 tests pass (15 new + 13 regression), all ACs verified | James (Dev) |
| 2026-02-23 | 1.2 | QA review PASS — wired redact() into ConfigService; 32/32 pass | Quinn (QA) |
| 2026-02-23 | 1.3 | apply-qa-fixes: all MINORs fixed by QA; deferred QA-1.3-04 noted; status → Done | James (Dev) |

## Dev Agent Record

### Agent Model Used

claude-4.6-sonnet-medium-thinking (James, Dev Agent)

### Debug Log References

- Verified `conf@15.1.0` still supports `encryptionKey` option (AES-256-CBC) — confirmed via node_modules readme
- Verified `dotenv@17.3.1` ESM pattern: `import dotenv from 'dotenv'` + `dotenv.config()` confirmed working
- Used `jest.unstable_mockModule` with per-instance fresh Map (in `mockImplementation`) — solves mock isolation without needing external reset mechanism
- Mocked `dotenv` to prevent real `.env` reads in tests
- `ENV_MAP` moved to `src/types/config.types.ts` (co-located with `AppConfig`) so both `ConfigService` and tests can import it without circular deps
- 28/28 tests pass on first run — no debug iterations needed

### Completion Notes List

- `conf@15.1.0` + `dotenv@17.3.1` installed as runtime `dependencies`
- `ENV_MAP` exported from `config.types.ts` — maps `AppConfig` keys to `TM_*` env var names; avoids magic strings in `ConfigService`
- `CONFIG_KEYS` exported for future iteration over config keys (Story 1.6 onboarding wizard)
- `configService` singleton exported from `config.service.ts` — ready for Story 1.6 command wiring
- `jest.unstable_mockModule` with fresh `Map` per `Conf` instantiation — each `new ConfigService()` in tests gets clean isolated state, no `beforeEach` reset needed
- `has()` checks env vars too (not just conf store) — consistent with `get()` precedence behaviour
- `delete()` only removes from conf store (env vars cannot be programmatically deleted this way)
- QA-1.3-01/02/03: all FIXED by Quinn directly in `src/services/config.service.ts` and `tests/services/config.service.test.ts`
- QA-1.3-04 (singleton pattern): deferred to Story 1.6 — same track as QA-1.2-03
- QA-1.3-05 (redundant cast): accepted, cosmetic only
- `npm run validate` ✅ — lint · typecheck · 32/32 tests · build

### File List

**New:**
- `src/types/config.types.ts` — `AppConfig` interface, `CONFIG_KEYS`, `ENV_MAP`
- `src/services/config.service.ts` — `ConfigService` class + `configService` singleton
- `src/utils/redact.ts` — `redact()` helper
- `tests/services/config.service.test.ts` — 10 unit tests (AC: 1, 3, 5)
- `tests/utils/redact.test.ts` — 5 unit tests (AC: 4)

**Modified:**
- `package.json` — `conf@^15.1.0` + `dotenv@^17.3.1` added to `dependencies`
- `package-lock.json` — updated

## QA Results

**Gate Decision: PASS**
Reviewed by: Quinn (QA Agent) — 2026-02-23
Gate file: `docs/qa/gates/1.3-configuration-service-with-encrypted-storage.yml`

### Issues Found and Resolved

| ID | Severity | Title | Status |
|----|----------|-------|--------|
| QA-1.3-01 | MINOR | `redact()` not wired into ConfigService (AC4 partial) | FIXED |
| QA-1.3-02 | MINOR | `initialize()` test only verified no-throw, not dotenv invocation | FIXED |
| QA-1.3-03 | MINOR | `apiKey` had no round-trip storage test | FIXED |
| QA-1.3-04 | CONCERNS | Module-level `configService` singleton | OPEN / Deferred to Story 1.6 |
| QA-1.3-05 | MINOR | Redundant type cast in `set()` | OPEN / Accepted (cosmetic) |

### Fixes Applied by QA

- **`src/services/config.service.ts`**: Added `import { redact }` + `getRedacted(key)` method — AC4 now actively enforced via a dedicated display-safe accessor
- **`tests/services/config.service.test.ts`**: Dynamic import of mocked dotenv; replaced weak initialize test with spy assertion; added `apiKey` round-trip; added 3 `getRedacted()` tests

### Final Validation

```
npm run validate →
  lint       ✅ — 0 errors
  typecheck  ✅ — clean
  tests      ✅ — 32/32 (28 original + 4 QA-added)
  build      ✅ — success
```

---

### Architecture-Compliance Re-Review: 2026-02-27

### Reviewed By: Quinn (Test Architect)

### Architectural Deviations Found in Story 1.3

| ID | Severity | Finding | Status |
|----|----------|---------|--------|
| ARCH-1.3-01 | CRITICAL | **Config schema too minimal**: Architecture (`database-schema.md`) defines config with `version`, `workspace_path`, `active_provider`, `confidence_threshold`, `process_log_enabled`, and nested `providers` map. `AppConfig` only had `provider` and `apiKey`. | FIXED in this review |
| ARCH-1.3-02 | CRITICAL | **Provider key name**: Architecture uses `active_provider` (not `provider`) as the canonical config key. Architecture config schema shows `"active_provider": "claude"`. | FIXED in this review — `active_provider` added to AppConfig; `provider` retained as deprecated alias |
| ARCH-1.3-03 | HIGH | **Config file location**: Architecture specifies `~/.config/tmr/config.json`. `conf` library stores in `~/Library/Application Support/@marlonvidal-tech-manager-os/` on macOS. | OPEN — conf library path differs from architecture spec. Architecture specifies explicit XDG path. Requires explicit path override in ConfigService constructor. |
| ARCH-1.3-04 | HIGH | **Missing ConfigService methods**: Architecture (`components.md`) defines `getActiveProvider()`, `setActiveProvider()`, `addProvider()`, `getWorkspacePath()`, `getConfidenceThreshold()`. None implemented. | OPEN — Documented Debt |
| ARCH-1.3-05 | HIGH | **Zod validation**: Architecture (`security.md`) mandates Zod schemas at all input boundaries. No Zod used anywhere in Epic 1. | OPEN — Documented Debt |
| ARCH-1.3-06 | MEDIUM | **`TmrError` base class**: Architecture (`error-handling-strategy.md`) requires `TmrError → ConfigurationError → ...` hierarchy. ConfigService errors were standalone. | FIXED in this review — TmrError hierarchy created |
| ARCH-1.3-07 | MEDIUM | **Env var prefix**: Architecture implicitly uses `TMR_` prefix for environment variables (aligned with binary `tmr`). Story used `TM_PROVIDER`/`TM_API_KEY`. | FIXED in this review — `TMR_PROVIDER` added to ENV_MAP for `active_provider` key |
| ARCH-1.3-08 | LOW | **`IConfigService` interface**: Architecture mandates `I` prefix for interfaces. No `IConfigService` interface defined. | OPEN — Documented Debt |

### Fixes Applied

- **`src/types/config.types.ts`**: `AppConfig` expanded with `version`, `workspace_path`, `active_provider`, `confidence_threshold`, `process_log_enabled`, `providers` (nested `ProviderConfig`). Added `CONFIG_DEFAULTS` with architecture-specified defaults. `ENV_MAP` now maps `active_provider → TMR_PROVIDER`, keeping `provider → TM_PROVIDER` as legacy alias.
- **`src/services/config.service.ts`**: `get()` and `has()` updated to handle the now-partial `ENV_MAP`. `getRedacted()` fixed to handle non-string config values.
- **`src/errors/tmr-error.ts`** (new file): Complete `TmrError` hierarchy with `ConfigurationError`, `FileSystemError`, `AIProviderError`, `ValidationError`, `RoutingError`, `TeamMemberNotFoundError`, `ProjectNotFoundError`, `InvalidEmailError`, `ConfidenceThresholdError`.

### Architecture Compliance Score

**75/100** (schema and error hierarchy fixed; config path, missing methods, and Zod are documented debt)

### Gate Status

Gate: **CONCERNS** → `docs/qa/gates/1.3-configuration-service-with-encrypted-storage.yml`
(Config file path, missing high-level methods, and Zod validation are open items requiring follow-up stories)

---

### Arch-Debt Dev Re-Review: 2026-02-26

### Reviewed By: Quinn (Test Architect)

### Changes Reviewed

Dev agent (James) applied arch-debt fixes touching this story:

- `src/services/config.service.ts` — config path fixed (`cwd: ~/.config/tmr`), 6 high-level domain methods added (`getActiveProvider`, `setActiveProvider`, `addProvider`, `getProviderConfig`, `getWorkspacePath`, `getConfidenceThreshold`)
- `src/utils/logger.ts` — NEW Winston 3.x logger utility (ARCH-DEBT-009)
- `tests/services/config.service.test.ts` — 14 new tests for domain API methods (46 total for config suite)

### Refactoring Performed by QA

- **File**: `src/services/config.service.ts`
  - **Change**: Renamed `addProvider` parameter `apiKeyEncrypted` → `apiKey`
  - **Why**: `conf` handles AES-256 encryption transparently — the caller does NOT pre-encrypt. The parameter name implied a pre-encryption responsibility that doesn't exist, which would mislead future callers.
  - **How**: Simple rename; also updated ProviderConfig JSDoc to clarify `api_key_encrypted` refers to the at-rest state, not caller responsibility.

- **File**: `src/utils/logger.ts`
  - **Change 1**: Added `mkdirSync(LOG_DIR, { recursive: true })` before `createLogger()`, guarded by `!IS_TEST`
  - **Why**: Winston File transport does NOT create parent directories — crashes on first run if `~/.config/tmr/logs/` doesn't exist. Would be a latent crash-on-first-run bug when logger is wired into services.
  - **How**: `mkdirSync` with `{ recursive: true }` is idempotent; wrapped in try/catch so the app degrades gracefully (console transport remains functional) if dir creation fails.
  - **Change 2**: File transport conditionally omitted when `NODE_ENV=test`
  - **Why**: Console transport was silenced in test mode but File transport was always created, causing real `~/.config/tmr/logs/tmr.log` writes during test runs.
  - **How**: Spread conditional: `...(!IS_TEST ? [new transports.File(...)] : [])`.

- **File**: `tests/services/config.service.test.ts`
  - **Change**: Updated test data strings from `'enc-key-abc'` → `'sk-ant-raw-key'` in addProvider tests
  - **Why**: The `'enc-'` prefix in old test values falsely implied callers pass pre-encrypted keys. Updated to realistic raw key strings that document the actual contract.

### Issues Found and Resolved

| ID | Severity | Title | Status |
|----|----------|-------|--------|
| QA-1.3-DEV-01 | MEDIUM | `addProvider` parameter `apiKeyEncrypted` — misleading name and doc | FIXED |
| QA-1.3-DEV-02 | HIGH | Logger File transport crashes on missing log directory | FIXED |
| QA-1.3-DEV-03 | MEDIUM | Logger File transport writes to real FS during tests | FIXED |

### Open Items Remaining

| ID | Severity | Title | Resolution |
|----|----------|-------|------------|
| ARCH-1.3-05 | HIGH | Zod validation at input boundaries | ARCH-DEBT-004 |
| ARCH-1.3-08 | LOW | `IConfigService` interface naming (I prefix) | ARCH-DEBT-010 |
| LOGGER-INTEGRATION | LOW | Logger not wired into ConfigService methods yet | Epic 2 |

### Final Validation

| Check | Result |
|---|---|
| `npm run validate` | ✅ 173/173 pass, 0 lint errors, clean typecheck, build |
| ARCH-1.3-03 (config path) | ✅ RESOLVED — `~/.config/tmr/config.json` |
| ARCH-1.3-04 (missing methods) | ✅ RESOLVED — 6 domain methods added + tested |
| ARCH-DEBT-009 (Winston logger) | ✅ RESOLVED — logger utility with test-mode safety |

### Architecture Compliance Score

**92/100** (up from 75 — config path and domain API both resolved; Zod and I-prefix are long-horizon debt)

### Gate Status

Gate: **PASS** → `docs/qa/gates/1.3-configuration-service-with-encrypted-storage.yml`
(Upgraded from CONCERNS — all blockers resolved; remaining open items are documented debt requiring dedicated stories)
