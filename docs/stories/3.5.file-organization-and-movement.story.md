# Story 3.5: File Organization and Movement

## Status

Ready for Review

## Story

**As a** developer,
**I want** files moved to correct destinations after categorization,
**so that** the workspace stays organized.

## Acceptance Criteria

1. `FileOrganizationService` with method: `organizeFile(file, destinations)`
2. Moves files from inbox to destination(s)
3. Creates destination directories if needed
4. Handles multi-destination cases (copy to multiple locations)
5. Archives unprocessable files to `archive/`
6. Atomic operations (no data loss)
7. Unit tests cover various organization scenarios

## Tasks / Subtasks

- [x] Task 1: Add organization result types (AC: 1, 6)
  - [x] Create `src/types/file-organization.types.ts`
  - [x] Define inline re-export note: `OrganizationResult` with `pathsWritten: string[]` (absolute paths), `mode: 'moved' | 'copied' | 'archived'`, optional `archivePath?: string`
  - [x] Document: `pathsWritten` is empty only on failure

- [x] Task 2: Extend `FileSystemService` (AC: 2, 4, 6)
  - [x] Add `removeFile(filePath: string): Promise<void>` — Node `fs.unlink` via fs-extra; throws `FileSystemError` on failure

- [x] Task 3: Implement `FileOrganizationService` (AC: 1–6)
  - [x] Create `src/services/file-organization.service.ts`
  - [x] Define `type Result<T> = { success: true; data: T } | { success: false; error: string }` inline (project pattern)
  - [x] Constructor: `(private readonly _fs: FileSystemService)`
  - [x] Private `_resolveDestDir(workspaceRoot: string, relativeDest: string): string | null` — normalize `relativeDest` (trim, strip leading `./`, accept trailing `/` as directory); `path.resolve(workspaceRoot, normalized)`; reject path traversal: if `path.relative(workspaceRoot, resolved)` starts with `..` or is absolute escape, return `null`
  - [x] Private `_uniqueTargetPath(destDir: string, basename: string): Promise<string>` — if `join(destDir, basename)` does not exist, return it; else return `join(destDir, {name}-{timestamp}{ext})` using `Date.now()` to avoid collisions
  - [x] Public `organizeFile(file: InboxFile, workspaceRoot: string, destinations: string[]): Promise<Result<OrganizationResult>>`:
    - [x] Validate `file.filepath` is inside `{workspaceRoot}/inbox` via `path.relative(inboxRoot, resolved)` non-empty and not `..`-escaped; if not → `{ success: false, error: '...' }`
    - [x] Normalize and dedupe destination list (case-sensitive unique dirs)
    - [x] **Unprocessable:** if `destinations.length === 0` OR all entries fail `_resolveDestDir` → treat as archive: `archiveDir = join(workspaceRoot, 'archive')`, `await _fs.createDirectory(archiveDir)`, compute unique path under `archive/` with same basename rules, `moveFile(inbox, archiveTarget)`, return `{ success: true, data: { pathsWritten: [archiveTarget], mode: 'archived', archivePath: archiveTarget } }`
    - [x] **Single destination:** `moveFile(file.filepath, targetPath)` after `createDirectory` on parent — target path is `_uniqueTargetPath(destDir, basename)`; return `mode: 'moved'`
    - [x] **Multiple destinations:** for each resolved dest dir, `targetPath = await _uniqueTargetPath(destDir, basename)`, `writeFile(targetPath, file.content)` (atomic per file — uses existing atomic `writeFile`); after **all** writes succeed, `removeFile(file.filepath)`; return `mode: 'copied'`, `pathsWritten` lists all targets
    - [x] On any failure after partial multi-write: return `{ success: false, error }` — partial copies may exist if a later write fails before inbox delete; inbox is only deleted after all writes succeed
    - [x] Never throw from `organizeFile`; always `Result`

- [x] Task 4: Unit tests (AC: 7)
  - [x] Create `tests/services/file-organization.service.test.ts`
  - [x] Mock `FileSystemService` with jest mocks for `createDirectory`, `moveFile`, `writeFile`, `removeFile`, `exists`
  - [x] Test: single destination → `moveFile` called once with correct dest; inbox path as source
  - [x] Test: multiple destinations → `writeFile` per dest with same content; `removeFile` on inbox after writes
  - [x] Test: empty destinations → archive under `archive/` + `moveFile` to archive path
  - [x] Test: invalid path `../outside` → archive or error per spec (reject traversal for destinations → if all invalid, archive)
  - [x] Test: `file.filepath` not under `inbox/` → `{ success: false }`
  - [x] Test: destination dirs created via `createDirectory` before write/move
  - [x] Test: deduplicated duplicate destination strings

- [x] Task 5: Integration test (optional but recommended)
  - [x] Create `tests/services/file-organization.service.integration.test.ts`
  - [x] Temp workspace with `inbox/foo.md`, real `FileSystemService`
  - [x] Single dest → file only in dest, not in inbox
  - [x] Multi dest → file in both dest dirs, inbox empty
  - [x] Empty dest → file in `archive/`

- [x] Task 6: Validate
  - [x] `npm run lint` → 0 problems
  - [x] `npm run typecheck` → 0 errors
  - [x] `npm test` → 710 tests pass, 47 suites
  - [x] `npm run build` → success

## Dev Notes

### Epic Context

Story 3.5 is the fifth story of Epic 3 (Process Intelligence Engine). It implements **physical filing** of inbox notes using `CategorizationResult.destinations` from Story 3.2.

- **Depends on:** Story 3.1 (`InboxFile` — complete), Story 3.2 (`destinations[]` workspace-relative paths — complete)
- **Consumed by:** Story 3.6 (`tmr process`) — orchestrator calls organization after categorization/context/tasks

[Source: docs/prd/epics/epic-3-process-intelligence-engine.md#story-35]

### Previous Story Insights (from 3.1–3.4)

- **Flat structure:** Code lives in `src/` at repo root — not the monorepo layout in `docs/architecture/source-tree.md`.
- **`Result<T>` inline** in the service file — no shared module.
- **Constructor DI:** inject `FileSystemService`; no singleton export for the service class (callers construct instances).
- **Private members:** `_fs` prefix.
- **File system:** Use `FileSystemService` throughout production code (matches existing services; ignore monorepo doc references to `FileSystemRepository`).

[Source: docs/stories/3.4.task-extraction-timeline-classification.story.md#previous-story-insights]

### Data Models

**`CategorizationResult.destinations`** — workspace-relative directory paths (may include trailing slash). Example: `my-teams/alpha/john@co.com/1on1s/`

[Source: docs/stories/3.2.ai-powered-categorization-logic.story.md#data-models]

**`InboxFile`** — `filepath` (absolute), `content`, `timestamp`

[Source: docs/stories/3.1.inbox-scanner-and-file-parser.story.md]

### Behavior Summary

| Case | Behavior |
|------|----------|
| 0 valid destinations (empty or all traversal-invalid) | Move inbox file to `archive/` with collision-safe basename |
| 1 destination | `moveFile` from inbox to `{dest}/{basename}` (unique if collision) |
| 2+ destinations | Atomic `writeFile` to each `{dest}/{basename}` using `file.content`, then `removeFile` inbox |

**Atomicity:** Per-file writes use existing atomic `writeFile` (temp + rename). Multi-destination cannot be a single filesystem transaction; worst case is partial writes if a later write fails — inbox is only removed after all writes succeed.

### File Locations

```
src/
├── types/
│   └── file-organization.types.ts  ← NEW
└── services/
    ├── file-system.service.ts      ← MODIFY (removeFile)
    └── file-organization.service.ts ← NEW

tests/
└── services/
    ├── file-organization.service.test.ts             ← NEW
    └── file-organization.service.integration.test.ts ← NEW (recommended)
```

### Testing

- **Framework:** Jest with `NODE_OPTIONS=--experimental-vm-modules`, `@jest/globals`
- **Location:** `tests/services/` mirroring `src/services/`
- **Patterns:** AAA; mock `FileSystemService` in unit tests; temp dir + real FS for integration

[Source: docs/architecture/coding-standards.md, docs/stories/3.4.task-extraction-timeline-classification.story.md#task-4]

### Technical Constraints

- No `console.log` in production code — use `logger` if logging is needed for errors (optional).
- Explicit return types on public methods.
- No `any`.

[Source: docs/architecture/coding-standards.md]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-08 | 1.0 | Initial draft for 3.5 | SM |
| 2026-04-08 | 1.1 | Implementation complete | Dev |

## Dev Agent Record

### Agent Model Used

Composer (Cursor agent)

### Debug Log References

None

### Completion Notes List

- Added `removeFile` to `FileSystemService` using `fs.unlink` (file-only deletes). All existing test mocks that implement `keyof FileSystemService` were updated to include `removeFile`.
- Inbox validation uses `path.relative(inboxRoot, resolvedFile)` — must be non-empty and must not escape via `..` (rejects the inbox directory itself as a “file” path).
- Multi-destination flow writes with atomic `writeFile` per target, then `removeFile` on the inbox path only after all writes succeed.
- Story DoD checklist self-review: requirements met, lint/typecheck/test/build green, tests at unit + integration level for organization service.

### File List

- `src/types/file-organization.types.ts` (new)
- `src/services/file-organization.service.ts` (new)
- `src/services/file-system.service.ts` (modified — `removeFile`)
- `tests/services/file-organization.service.test.ts` (new)
- `tests/services/file-organization.service.integration.test.ts` (new)
- `tests/services/file-system.service.test.ts` (modified — `unlink` mock + `removeFile` tests)
- `tests/services/*.test.ts` (modified — `removeFile` on MockFS for `task`, `email-resolution`, `project`, `team`, `context`, `inbox`, `leadership`, `member`, `relationship`, `section-parser`)

## QA Results

### Review date: 2026-04-08

### Reviewed by: Quinn (Test Architect)

### Requirements traceability

| AC | Coverage |
|----|----------|
| 1 `FileOrganizationService.organizeFile` | Unit + integration |
| 2 Move from inbox | Single-dest `moveFile`; multi-dest write + `removeFile` inbox |
| 3 Create destination dirs | `createDirectory` before each placement |
| 4 Multi-destination | `writeFile` per dest + integration test |
| 5 Archive | Empty or all-invalid destinations → `archive/` |
| 6 Atomic ops | Per-file atomic `writeFile`; inbox deleted only after all multi-writes |
| 7 Unit tests | `file-organization.service.test.ts` + integration suite |

### Gate decision

See `docs/qa/gates/3.5-file-organization-and-movement.yml` — **PASS**.

### Notes

- Low risk: filesystem-only feature, no network/auth. Regression coverage extended via `removeFile` mock updates across services.
- Optional follow-up for Story 3.6: wire `FileOrganizationService` into `tmr process` orchestration.
