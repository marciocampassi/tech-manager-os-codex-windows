# Story 3.1: Inbox Scanner and File Parser

## Status

Ready for Done

## Story

**As a** developer,
**I want** to scan and parse inbox files efficiently,
**so that** the process command can handle them.

## Acceptance Criteria

1. `InboxService` class with methods: `scanInbox()`, `parseFile()`
2. Supports file types: `.txt`, `.md`, `.json`
3. Returns array of `InboxFile` objects with: filepath, content, timestamp
4. Handles edge cases: empty inbox, large files, malformed files
5. Sorted by timestamp (oldest first)
6. Unit tests cover various file types and scenarios

## Tasks / Subtasks

- [x] Task 1: Define `InboxFile` interface and inbox types (AC: 3)
  - [x] Create `src/types/inbox.types.ts` with `InboxFile` interface (fields: `filepath`, `content`, `timestamp`)
  - [x] Add `SUPPORTED_EXTENSIONS` constant for `.txt`, `.md`, `.json`
  - [x] Add `MAX_FILE_SIZE_BYTES` constant (10 MB limit for large-file guard)

- [x] Task 2: Implement `InboxService` class (AC: 1, 2, 3, 4, 5)
  - [x] Create `src/services/inbox.service.ts` with class `InboxService`
  - [x] Constructor accepts `FileSystemService` via DI
  - [x] Implement `scanInbox(workspaceRoot: string): Promise<Result<InboxFile[]>>`
    - [x] Call `fileSystemService.listFiles(inboxDir)` for each supported extension
    - [x] Handle empty inbox (return `{ success: true, data: [] }`)
    - [x] Filter to only `.txt`, `.md`, `.json` extensions
    - [x] Collect `mtime` (file modified time via `fs.stat`) for each file
    - [x] Sort by timestamp ascending (oldest first)
    - [x] Return array of `InboxFile` objects
  - [x] Implement `parseFile(filePath: string): Promise<Result<InboxFile>>`
    - [x] Read file content via `fileSystemService.readFile()`
    - [x] Get file `mtime` via `fs.stat()`
    - [x] Handle malformed/unreadable files — return `{ success: false, error: ... }`
    - [x] Handle large files (> 10 MB) — return `{ success: false, error: 'File too large' }`
    - [x] Return `{ success: true, data: InboxFile }` on success
  - [x] Export singleton `inboxService`

- [x] Task 3: Unit tests (AC: 6)
  - [x] Create `tests/services/inbox.service.test.ts`
  - [x] Test `scanInbox()`:
    - [x] Empty inbox returns `{ success: true, data: [] }`
    - [x] Returns only `.txt`, `.md`, `.json` files (ignores `.pdf`, `.png`, etc.)
    - [x] Results sorted by timestamp ascending
    - [x] `listFiles` throwing → `{ success: false, error: ... }`
  - [x] Test `parseFile()`:
    - [x] `.md` file parsed successfully with correct `filepath`, `content`, `timestamp`
    - [x] `.txt` file parsed successfully
    - [x] `.json` file parsed successfully
    - [x] File > 10 MB returns `{ success: false, error: 'File too large: ...' }`
    - [x] Unreadable file (readFile throws) returns `{ success: false, error: ... }`
    - [x] `stat` failure returns `{ success: false, error: ... }`

- [x] Task 4: Validate
  - [x] `npm run lint` → 0 problems
  - [x] `npm run typecheck` → 0 errors
  - [x] `npm test` → all tests pass (515 tests, 35 suites)
  - [x] `npm run build` → dist/cli.js 113.76 KB ✓

## Dev Notes

### Epic Context

This is Story 3.1 — the first story of Epic 3 (Process Intelligence Engine). It establishes the foundational `InboxService` that all subsequent Epic 3 stories (`3.2` Categorization, `3.3` Context Updates, `3.4` Task Extraction, `3.5` File Organization, `3.6` `tmr process` command) depend on.

No previous Epic 3 story exists. Key insights from the last completed Epic 2 story (2.8):
- The project uses `npm run validate` = lint + typecheck + test + build.
- `fileSystemService.writeFile` implicitly creates parent dirs via `fs.ensureDir`.
- The test framework uses `@jest/globals` + `jest.unstable_mockModule` for ESM-safe mocking.
- Tests follow: import mocks first → `jest.unstable_mockModule(...)` → dynamic import → `beforeEach` reset.

### File Locations

```
src/
├── types/
│   └── inbox.types.ts          ← NEW
└── services/
    └── inbox.service.ts        ← NEW

tests/
└── services/
    └── inbox.service.test.ts   ← NEW
```

Source tree reference: flat structure (`src/` at repo root), not monorepo packages.
[Source: docs/architecture/source-tree.md]

### Data Models

`InboxFile` object shape (per AC 3):

```typescript
interface InboxFile {
  filepath: string;   // Absolute path to the file
  content: string;    // Raw file content as UTF-8 string
  timestamp: Date;    // File mtime — used for sort order (oldest first)
}
```

[Source: docs/prd/epics/epic-3-process-intelligence-engine.md#story-31]

### FileSystemService API Available

All file I/O MUST go through `FileSystemService` (constructor-injected). Its relevant methods:

```typescript
listFiles(dirPath: string, extension?: string): Promise<string[]>
// Returns absolute paths of top-level files, optionally filtered by extension.
// Throws FileSystemError if directory doesn't exist.

readFile(filePath: string): Promise<string>
// Returns UTF-8 content. Throws FileSystemError on I/O error.
```

For `mtime`, use raw `fs-extra` stat — the service doesn't expose a `stat` method.
Import pattern: `import * as fsNs from 'fs-extra'` with the CJS/ESM interop shim already established in `file-system.service.ts`.

[Source: src/services/file-system.service.ts]

### Result<T> Wrapper

ALL public async methods that can fail MUST return `Result<T>` — never throw from async methods:

```typescript
type Result<T> =
  | { success: true; data: T }
  | { success: false; error: string };
```

[Source: docs/architecture/coding-standards.md#critical-rules]

### Naming Conventions

- File: `inbox.service.ts` (kebab-case)
- Class: `InboxService` (PascalCase)
- Interface: `IInboxFile` or just `InboxFile` — project uses both patterns; follow the `InboxFile` (no prefix) convention for domain types per existing `member.types.ts` pattern
- Private members: `_fs` (underscore prefix)
- Constants: `SUPPORTED_EXTENSIONS`, `MAX_FILE_SIZE_BYTES` (UPPER_SNAKE_CASE)

[Source: docs/architecture/coding-standards.md#naming-conventions]

### Inbox Path

The workspace `inbox/` directory lives at `{workspaceRoot}/inbox/`. This is established by `tmr init` via `workspace-builder.ts`. The `InboxService` should receive `workspaceRoot` as a parameter (not hardcode it) to remain testable.

```typescript
const inboxDir = path.join(workspaceRoot, 'inbox');
```

[Source: src/workflows/workspace-builder.ts, docs/architecture/source-tree.md]

### Supported File Types (AC: 2)

```typescript
const SUPPORTED_EXTENSIONS = ['.txt', '.md', '.json'] as const;
```

Scan strategy: call `listFiles(inboxDir, ext)` for each extension, then merge + sort.

### Large File Guard (AC: 4)

```typescript
const MAX_FILE_SIZE_BYTES = 10 * 1024 * 1024; // 10 MB
```

Check file size via `fs.stat(filePath).size` before reading. If `size > MAX_FILE_SIZE_BYTES`, return `{ success: false, error: \`File too large: ${filePath} (${size} bytes)\` }`.

### Error Handling for Edge Cases (AC: 4)

- **Empty inbox**: `listFiles` returns `[]` for each extension → `scanInbox` returns `{ success: true, data: [] }`. Do NOT treat this as an error.
- **Malformed files**: A `.json` file with invalid JSON is still "parseable" as raw text for this story — `parseFile` returns the raw string content. The categorization service (Story 3.2) is responsible for semantic parsing. So `parseFile` should NOT attempt JSON.parse.
- **Large files**: Guard in `parseFile`, return error result.
- **Unreadable files (fs errors)**: Catch `FileSystemError` from `readFile`, return `{ success: false, error: err.message }`.
- **`stat` errors**: Catch and return `{ success: false, error: ... }`.

### Testing Standards

- **Framework**: Vitest 1.x (project config) via `@jest/globals` compatibility shim
- **Test file**: `tests/services/inbox.service.test.ts`
- **Pattern**: AAA (Arrange, Act, Assert)
- **Mocking**: `jest.unstable_mockModule('fs-extra', ...)` before dynamic import of the service
- **Mock `fs.stat`**: needed for mtime and size; add to the mock factory
- **Mock `FileSystemService`**: inject a mock instance directly into `InboxService` constructor — no need to mock `fs-extra` for `FileSystemService` itself; create a plain object mock with `jest.fn()` for `listFiles` and `readFile`
- **Coverage target**: 90% (core package standard)
- **Test file location**: `tests/services/` (mirrors `src/services/`)

[Source: docs/architecture/test-strategy-and-standards.md]

### Testing

- Test file location: `tests/services/inbox.service.test.ts`
- Test standards: AAA pattern, Vitest via `@jest/globals`, `jest.unstable_mockModule` for ESM mocking
- Mock `fs-extra` for `stat` calls; mock `FileSystemService` methods directly via constructor injection
- Cover: empty inbox, supported extensions, unsupported extension filtering, sort order, large file error, unreadable file error, stat failure, all 3 file types (.txt/.md/.json)
- Coverage minimum: 90% for core services

## Story Draft Checklist Results

| Category                             | Status  | Issues |
| ------------------------------------ | ------- | ------ |
| 1. Goal & Context Clarity            | PASS    | Epic dependency chain clearly explained; business value (foundation for `tmr process`) stated |
| 2. Technical Implementation Guidance | PASS    | File locations, `FileSystemService` API, `Result<T>` pattern, `InboxFile` shape, constants all explicit |
| 3. Reference Effectiveness           | PASS    | Source citations on each technical section; prior story context (2.8) summarized in-line |
| 4. Self-Containment Assessment       | PASS    | All edge cases documented (empty, large, malformed, unreadable); no external docs required |
| 5. Testing Guidance                  | PASS    | Mock strategy, file location, coverage target, all scenarios enumerated |

**Final Assessment:** READY — Story provides sufficient context for implementation. Clarity score: 9/10.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-18 | 1.0 | Initial draft | Bob (SM) |
| 2026-03-18 | 1.1 | Implementation complete — all tasks done, 515 tests green | James (Dev) |
| 2026-03-18 | 1.2 | QA PASS (96/100) — no code changes required; 3 future recommendations deferred to downstream stories | Quinn (QA) + James (Dev) |

## Dev Agent Record

### Agent Model Used

claude-4.6-sonnet-medium-thinking

### Debug Log References

- `npm run lint` → ✓ 0 problems
- `npm run typecheck` → ✓ 0 errors
- `npm test` → ✓ 515 tests passing (35 suites); new suite: 13/13 pass
- `npm run build` → ✓ dist/cli.js 113.76 KB

### Completion Notes List

- `InboxFile` type and constants defined in `src/types/inbox.types.ts`; no `I` prefix per project domain-type convention (matches `member.types.ts` pattern)
- `InboxService.scanInbox()` iterates over all 3 supported extensions sequentially, silently skips unparseable files during batch scan (caller gets all valid files), returns error only on `listFiles` failure
- `InboxService.parseFile()` uses direct `fs.stat` via the same CJS/ESM interop shim as `file-system.service.ts` for `mtime` and size — `FileSystemService` has no `stat` method
- `Result<T>` type defined inline in service (not imported from shared module — consistent with existing pattern where no shared `Result<T>` module exists yet)
- Tests mock `fs-extra` via `jest.unstable_mockModule` (for `stat`) and inject a mock `FileSystemService` directly via constructor — avoids double-mocking `fs-extra` for service methods
- 13 new tests cover all 6 ACs plus non-Error rejection branches for defensive string-error handling

### File List

**Created:**
- `src/types/inbox.types.ts` — `InboxFile` interface, `SUPPORTED_EXTENSIONS`, `MAX_FILE_SIZE_BYTES`
- `src/services/inbox.service.ts` — `InboxService` class + `inboxService` singleton
- `tests/services/inbox.service.test.ts` — 13 unit tests

## QA Results

### Review Date: 2026-03-18

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Clean, idiomatic implementation. `InboxService` follows the established DI constructor-injection pattern, uses the `Result<T>` wrapper consistently, and the CJS/ESM interop shim for `fs.stat` is correctly mirrored from `file-system.service.ts`. The large-file guard (10 MB) is a good defensive measure. The decision to treat `parseFile` as raw-string-only (no JSON.parse) is correctly deferred to Story 3.2's Categorization Service and is well-documented in comments and the types file.

### Refactoring Performed

None required. Code is clean as submitted.

### Compliance Check

- Coding Standards: ✓ — `Result<T>`, no `any`, PascalCase classes, kebab-case files, explicit return types, `_fs` prefix, no `console.log`
- Project Structure: ✓ — `src/types/inbox.types.ts`, `src/services/inbox.service.ts`, `tests/services/inbox.service.test.ts`
- Testing Strategy: ✓ — AAA pattern, `jest.unstable_mockModule`, 13 unit tests, FileSystemService injected as mock
- All ACs Met: ✓ — All 6 ACs verified below

### AC Verification

| AC | Status | Notes |
|----|--------|-------|
| 1  | ✅ PASS | `InboxService` with `scanInbox()` and `parseFile()` implemented and exported |
| 2  | ✅ PASS | `.txt`, `.md`, `.json` enumerated in `SUPPORTED_EXTENSIONS`; scanned per extension |
| 3  | ✅ PASS | `InboxFile` interface with `filepath: string`, `content: string`, `timestamp: Date` |
| 4  | ✅ PASS | Empty inbox → `{ success: true, data: [] }`; large files → error result; malformed/unreadable → error result |
| 5  | ✅ PASS | `.sort((a,b) => a.timestamp.getTime() - b.timestamp.getTime())` — ascending |
| 6  | ✅ PASS | 13 unit tests: all 3 file types, empty inbox, sort order, large file, stat fail, readFile fail, non-Error rejections, skip-bad-files |

### Improvements Checklist

- [x] All ACs verified — no gaps
- [x] Edge case coverage complete (empty, large, unreadable, non-Error rejections)
- [ ] Consider extracting `Result<T>` to `src/types/shared.types.ts` when Epic 3 accumulates more services (non-blocking — consistent with current project pattern)
- [ ] `SupportedExtension` type is exported but not yet consumed externally — Story 3.2 should use it for type-safe extension checks (non-blocking)
- [ ] Future: add logging of skipped files in `scanInbox` for observability when `tmr process --verbose` is implemented in Story 3.6

### Security Review

No security concerns. No PII handling, no API calls, no credential exposure. The 10 MB large-file guard prevents memory exhaustion from malicious or oversized inbox files. File path is caller-provided (workspace root); path traversal is an architectural concern for the CLI layer, not this service.

### Performance Considerations

Sequential extension scanning is appropriate for MVP. `parseFile` reads content only after a size check, avoiding unnecessary I/O for oversized files. For Story 3.6's `tmr process --dry-run`, `parseFile` could be called with a size-only fast-path (future optimization).

### Files Modified During Review

None — no refactoring required.

### Gate Status

Gate: PASS → docs/qa/gates/3.1-inbox-scanner-and-file-parser.yml
Quality Score: 96/100

### Recommended Status

✓ Ready for Done
