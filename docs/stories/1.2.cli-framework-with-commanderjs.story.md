# Story 1.2: CLI Framework with Commander.js

## Status

Done

## Story

**As a** user,
**I want** a CLI that displays help and handles commands properly,
**so that** I can discover available commands and understand usage.

## Acceptance Criteria

1. Commander.js integrated in `src/cli.ts`
2. `tm --help` displays tool description, version, and command list
3. `tm --version` displays current version (`1.0.0`)
4. `tm` with no args shows help
5. Unknown commands show error with suggestion to run `tm --help`
6. Global flags registered and parsed: `--verbose`, `--plain`, `--json`
7. Error handling with user-friendly messages (no raw stack traces to end user)

> **Note:** The epic file references `tmr --help` / `tmr --version`. This is a PRD typo — the canonical binary name is `tm` per `package.json` `"bin": { "tm": "./dist/cli.js" }` and `docs/prd/technical-assumptions.md`. All ACs use `tm`.

## Tasks / Subtasks

- [x] Task 1: Install Commander.js runtime dependency (AC: 1)
  - [x] `npm install commander`
  - [x] `npm install -D @types/node` (already installed, verify)
  - [x] Verify `commander` is in `dependencies` (not `devDependencies`) — it ships with the binary

- [x] Task 2: Refactor `src/cli.ts` — integrate Commander.js (AC: 1, 2, 3, 4)
  - [x] Import `Command` from `commander`
  - [x] Create root `program` with name `tm`, description, and version from package constant
  - [x] Set `.helpOption('-h, --help', 'Show help')` and `.addHelpCommand(false)` (no help subcommand)
  - [x] Configure `tm` with no args → show help automatically (`.addHelpText` or parse behaviour)
  - [x] Export `program` for testability

- [x] Task 3: Register global flags (AC: 6)
  - [x] `--verbose` → enable verbose logging
  - [x] `--plain` → disable colors/formatting (accessibility)
  - [x] `--json` → output as machine-readable JSON
  - [x] Store parsed options in a shared `GlobalOptions` TypeScript interface
  - [x] Export `GlobalOptions` type for use in later stories

- [x] Task 4: Register stub commands (AC: 2)
  - [x] Register `init` command stub (implemented in Story 1.6)
  - [x] Register `process` command stub (implemented in Epic 2)
  - [x] Register `watch` command stub (implemented in Epic 2)
  - [x] Each stub outputs: `"Command 'X' coming soon — run 'tm --help' for available commands"`
  - [x] Commands appear in `tm --help` output with their descriptions

- [x] Task 5: Unknown command handling (AC: 5)
  - [x] Use `.on('command:*', ...)` or Commander's `.addCommand` with unknown-command handler
  - [x] Output user-friendly error: `Unknown command: 'X'. Run 'tm --help' to see available commands.`
  - [x] Exit with code `1` on unknown commands

- [x] Task 6: Global error handling (AC: 7)
  - [x] Wrap `program.parseAsync()` in try/catch
  - [x] On error: print user-friendly message to stderr (no raw stack traces unless `--verbose`)
  - [x] Exit with code `1` on unhandled errors

- [x] Task 7: Write unit tests (AC: 1–7)
  - [x] Update `tests/cli.test.ts` — replace VERSION smoke test with CLI tests
  - [x] Test: program name is `tm`
  - [x] Test: `--version` flag returns `1.0.0`
  - [x] Test: `--help` flag exits without error
  - [x] Test: `--verbose`, `--plain`, `--json` flags are parsed correctly
  - [x] Test: unknown command triggers error handler
  - [x] Use Commander's `program.parseAsync(['node', 'tm', ...args])` pattern for testability

- [x] Task 8: Validate end-to-end (AC: 2, 3, 4, 5)
  - [x] `npm run build` → `dist/cli.js`
  - [x] `node dist/cli.js --help` → shows help
  - [x] `node dist/cli.js --version` → `1.0.0`
  - [x] `node dist/cli.js` → shows help (no args)
  - [x] `node dist/cli.js foobar` → prints unknown command error, exits 1

## Dev Notes

### Previous Story Insights (from Story 1.1)

- **ESM is active**: `"type": "module"` in `package.json`. All imports must use `.js` extension in source (e.g., `import { program } from './cli.js'` in tests). This is already handled by `jest.config.ts` `moduleNameMapper`.
- **tsup handles the shebang**: Do NOT add `#!/usr/bin/env node` to `src/cli.ts` — tsup's `banner.js` config already prepends it to `dist/cli.js`.
- **ESM + Jest**: Test script uses `NODE_OPTIONS=--experimental-vm-modules jest`. Test imports use `.js` extensions (mapped back by `moduleNameMapper`).
- **Binary name is `tm`**: The epic PRD says `tmr` in some places — this is a typo. Always use `tm`.
- **ESLint flat config**: `eslint.config.js` with `@typescript-eslint`. `files: ['src/**/*.ts']` scoping is in place. `no-console` is `warn` — use `process.stdout.write` or suppress in CLI output paths.
- **jest@29.7.0 + ts-jest@29.4.6**: Correct installed versions — do not upgrade without checking both together.

[Source: docs/stories/1.1.project-scaffolding-and-typescript-configuration.story.md#completion-notes-list]

### Binary Name Clarification

```
Binary: tm   (NOT tmr)
Installed via: "bin": { "tm": "./dist/cli.js" } in package.json
Run locally:   node dist/cli.js --help
Run globally:  tm --help (after npm install -g)
```

[Source: docs/prd/technical-assumptions.md#build--distribution]

### Commander.js Integration Pattern

```typescript
import { Command } from 'commander';
import { createRequire } from 'module';

const require = createRequire(import.meta.url);
const pkg = require('../package.json') as { version: string };

export const program = new Command();

program
  .name('tm')
  .description('AI-powered CLI for engineering managers')
  .version(pkg.version, '-v, --version')
  .option('--verbose', 'enable verbose output')
  .option('--plain', 'disable colors and formatting')
  .option('--json', 'output as JSON');
```

> **Why `createRequire`?** ESM does not support `import json` by default without `--experimental-json-modules`. Using `createRequire` for JSON is the stable ESM-compatible pattern for Node 18+. `tsconfig.json` already has `"resolveJsonModule": true` for type checking purposes.

[Source: docs/prd/technical-assumptions.md#cli]

### GlobalOptions Interface

Define and export this interface — it will be consumed by every command in later stories:

```typescript
export interface GlobalOptions {
  verbose?: boolean;
  plain?: boolean;
  json?: boolean;
}
```

### File Locations

```
src/
└── cli.ts          ← MODIFY: add Commander setup, global flags, stub commands, error handling

tests/
└── cli.test.ts     ← MODIFY: replace VERSION smoke test with Commander unit tests
```

No new directories needed for this story. Commands get their own `src/commands/` folder in Story 1.6+.

[Source: docs/prd/epic-details.md#story-12]

### Runtime vs Dev Dependencies

- `commander` → **`dependencies`** (runtime — ships in the npm package)
- Everything else already in `devDependencies`

[Source: docs/prd/technical-assumptions.md#cli]

### Testing Pattern for Commander

Commander's `program.parseAsync()` requires `['node', 'tm', ...args]` as the argv array. To prevent `process.exit()` from killing tests, use `.exitOverride()`:

```typescript
program.exitOverride(); // throws instead of calling process.exit()

await expect(program.parseAsync(['node', 'tm', '--version']))
  .rejects.toThrow(); // Commander throws on --version with exitOverride
```

Alternatively, spy on `process.exit` with Jest:
```typescript
const exitSpy = jest.spyOn(process, 'exit').mockImplementation(() => { throw new Error('exit'); });
```

### Performance

- CLI startup target: <100ms (excluding AI calls)
- Commander adds ~2ms overhead — acceptable
- Importing `commander` must not trigger any heavy I/O at startup

[Source: docs/prd/technical-assumptions.md#performance-constraints]

## Testing

- **Framework:** Jest 29 with `ts-jest` (ESM mode)
- **Test file:** `tests/cli.test.ts` (replace existing smoke test)
- **Test pattern:** Use `program.parseAsync(['node', 'tm', ...args])` with `.exitOverride()` to prevent `process.exit` from aborting the test runner
- **Coverage:** Target all 7 ACs with at least one test each
- **No integration tests** in this story — that comes in Story 1.6 (`tmr init` full flow)

[Source: docs/prd/technical-assumptions.md#testing-requirements]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-02-23 | 1.1 | Implemented — 12/12 tests pass, all ACs verified | James (Dev) |
| 2026-02-23 | 1.2 | QA review PASS — fixed stdout leak + AC4 test gap; 13/13 pass | Quinn (QA) |
| 2026-02-23 | 1.3 | apply-qa-fixes: documented QA-1.2-04 guard assumption in src/cli.ts; all OPEN items resolved or deferred | James (Dev) |

## Dev Agent Record

### Agent Model Used

claude-4.6-sonnet-medium-thinking (James, Dev Agent)

### Debug Log References

- Initial test run failed: `jest is not defined` — ESM mode requires `@jest/globals` imports, not global `jest`
- Second issue: `await run()` at module top-level fired on import during tests — resolved with `JEST_WORKER_ID` guard
- Third issue: `eslint.config.js` missing `globals.node` — `process` was flagged as `no-undef`; fixed by installing `globals` package and adding `globals.node` to languageOptions
- QA apply-qa-fixes run: gate PASS, both MINORs already fixed by QA. Applied QA-1.2-04 recommendation: added inline comment to `src/cli.ts` documenting the `JEST_WORKER_ID` guard assumption and migration path

### Completion Notes List

- `commander@14.0.3` installed as runtime `dependency` (not devDependency) — confirmed in `package.json`
- Used `createProgram()` factory function pattern instead of singleton export — enables fresh program per test, avoiding shared state between test cases
- `JEST_WORKER_ID` env guard wraps `await run()` — prevents module-level execution during test imports
- ESM JSON import via `createRequire(import.meta.url)` — stable Node 18+ pattern, no `--experimental-json-modules` needed
- Exported `GlobalOptions` interface — consumed by all future command implementations
- `eslint.config.js` updated: added `globals.node` to fix `process` no-undef errors; also installed `globals` devDependency
- 12/12 tests pass covering all 7 ACs (QA raised to 13/13 — see QA Results)
- QA-1.2-01 (stdout leak) and QA-1.2-02 (AC4 unit test) both fixed by Quinn in `tests/cli.test.ts`
- QA-1.2-03 (run() singleton coupling) deferred to Story 1.6 per gate
- QA-1.2-04 (JEST_WORKER_ID guard): documented assumption inline in `src/cli.ts` — accepted risk
- `npm run lint` ✅ · `npm test` ✅ 13/13 · `npm run build` ✅ 2.14KB · `tsc --noEmit` ✅

### File List

**Modified:**
- `src/cli.ts` — full rewrite: Commander setup, GlobalOptions, createProgram factory, stub commands, unknown command handler, error handler, JEST_WORKER_ID guard + QA-1.2-04 inline comment
- `tests/cli.test.ts` — full rewrite: 13 unit tests covering all 7 ACs (QA added stdout spies + AC4 test)
- `eslint.config.js` — added `globals.node` to languageOptions
- `package.json` — `commander@^14.0.3` added to `dependencies`; `globals` added to `devDependencies`
- `package-lock.json` — updated

## QA Results

**Gate Decision: PASS**
Reviewed by: Quinn (QA Agent) — 2026-02-23
Gate file: `docs/qa/gates/1.2-cli-framework-with-commanderjs.yml`

### Issues Found and Resolved

| ID | Severity | Title | Status |
|----|----------|-------|--------|
| QA-1.2-01 | MINOR | Test stdout leak — global flags tests wrote to real stdout | FIXED |
| QA-1.2-02 | MINOR | AC4 had no unit test — only e2e verified | FIXED |
| QA-1.2-03 | CONCERNS | `run()` hardwired to module-level singleton — not unit testable | OPEN / Deferred to Story 1.6 |
| QA-1.2-04 | CONCERNS | `JEST_WORKER_ID` guard couples production code to test infra | OPEN / Accepted risk |

### Fixes Applied by QA

- **`tests/cli.test.ts`**: Added `jest.spyOn(process.stdout, 'write').mockImplementation()` to 5 tests that trigger command output (global flags + help/version) — eliminates output noise during test runs
- **`tests/cli.test.ts`**: Added `'no-args shows help output (AC4)'` unit test to cover the `argv.length <= 2` guard that was only e2e verified

### Final Validation

```
npm run lint   ✅ — 0 errors, 0 warnings
npm test       ✅ — 13/13 tests pass (12 original + 1 QA-added)
npm run build  ✅ — dist/cli.js 2.14KB
tsc --noEmit   ✅ — clean
```
